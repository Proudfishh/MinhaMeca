{
    "private": true,
    "type": "module",
    "scripts": {
        "prepare": "git config core.hooksPath .githooks || exit 0",
        "dev": "vite",
        "build": "vite build",
        "start": "concurrently --kill-others --names \"laravel,vite\" --prefix-colors \"blue,green\" \"php artisan serve --host=0.0.0.0 --port=8000\" \"vite\""
    },
    "devDependencies": {
        "@playwright/test": "^1.61.0",
        "@tailwindcss/vite": "^4.3.1",
        "axios": "^1.6.4",
        "concurrently": "^10.0.3",
        "laravel-vite-plugin": "^1.0",
        "playwright": "^1.61.0",
        "tailwindcss": "^4.3.1",
        "vite": "^5.0"
    },
    "allowScripts": {
        "esbuild@0.21.5": true
    }
}
